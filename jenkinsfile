pipeline {
    agent {
        docker { image 'docker:dind' }
    }

    stages {
        stage('Test') {
            steps {
                sh 'node --eval "console.log(process.platform,process.env.CI)"'
            }
        }
    }
}