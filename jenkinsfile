/* Requires the Docker Pipeline plugin */
pipeline {
    agent { docker { image 'docker:dind' } }
    stages {
        stage('compose') {
            steps {
                sh 'ls',
                sh 'docker compose up'
            }     
        }
    }
}